# Supabase Configuration
# Get these from your Supabase Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# GitHub OAuth Configuration
# Get these from your GitHub Settings > Developer settings > OAuth Apps
AUTH_GITHUB_ID=your_github_client_id
AUTH_GITHUB_SECRET=your_github_client_secret

# Auth.js Secret
# Run `openssl rand -base64 32` to generate a secret key
AUTH_SECRET=your_random_secret_key

# 计费方案 (Token Plan) 与 模型选择 (MiniMax-M2.1)
# 获取地址: https://platform.minimaxi.com/user-center/token-plan
MINIMAX_API_KEY=your_minimax_api_key
MINIMAX_MODEL=MiniMax-M2.1
# Group ID 仅老版企业计费需要，Token计费模式通常留空
MINIMAX_GROUP_ID=

# App Configuration
# For local dev: http://localhost:3000
# For production: https://your-domain.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000
